{% extends 'base.html.twig' %}
{% block title %}Registrarse, {{ parent() }}{% endblock %}
{% block body %}
    <section class="page_breadcrumbs ds parallax section_padding_75" style="background-position: 50% 1px;">
        <div class="container">
            <div class="row">
                <div class="col-sm-12 text-center">
                    <h2>Registrar un usuario nuevo</h2>
                    <p>Anunciarse gratis</p>
                    <ol class="breadcrumb greylinks">
                        <li>
                            <a href="/">Inicio</a>
                        </li>
                        <li>
                            <a href="{{ path('login') }}">Iniciar sesión</a>
                        </li>
                        <li class="active">Registrarse</li>
                    </ol>
                </div>
            </div>
        </div>
    </section>

    <div class="container" style="margin-top:111px; margin-bottom: 111px; " >
        <div class="row">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}